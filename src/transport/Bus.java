package transport;

public class Bus extends Transport{

    public Bus(String brand, String model, String color, int year, String productionCountry, Integer maxSpeed) {
        super(brand, model, color, year, productionCountry, maxSpeed);
    }

}
